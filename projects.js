document.addEventListener("DOMContentLoaded", () => {
    const projectSection = document.querySelector(".card-container");
  
    // Add the animation class with a slight delay
    setTimeout(() => {
      projectSection.classList.add("show");
    }, 200); // you can adjust the delay if needed
  });
  
  